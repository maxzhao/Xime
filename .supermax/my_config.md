五笔拼音混拼：

- 默认状态为英文
- 中英文切换使用 右shift
- 切换英文状态时,保留输入窗口中已存在的字符并上屏
- 中文/英文标点切换使用快捷键: Ctrl + 句号
- 候选字词，有以下要求：
  翻页按键为左右方括号
  二三候选使用分号单引号

五笔输出时要求：

- 编码逐键提示
- 四码唯一时自动上屏
- 第五码将首选上屏

拼音输入时要求：
模糊音支持z=zh, c=ch,s=sh,an=ang,en=eng,in=ing,ian=iang,uan=uang
